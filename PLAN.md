# TinkerDesk - План разработки

## Обзор проекта

Веб-приложение для голосовых созвонов команды (аналог Zoom) с функциями:
- Голосовая связь через LiveKit
- Запись каждого участника отдельно
- Транскрибация речи в текст (Parakeet/NVIDIA NeMo)
- Суммаризация встреч (Claude API)
- Формирование диалога с именами участников

## Технологический стек

| Компонент | Технология |
|-----------|------------|
| Frontend | Next.js 14 (App Router) |
| Стилизация | Tailwind CSS + shadcn/ui |
| База данных | PostgreSQL + Prisma ORM |
| Голосовая связь | LiveKit (self-hosted) |
| Транскрибация | Parakeet V3 (Rust/ONNX, CPU-оптимизирован) |
| Суммаризация | Claude API (Anthropic) |
| Хранение файлов | MinIO (S3-совместимое) |
| Контейнеризация | Docker + Docker Compose |

## Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend (Next.js)                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │
│  │  Лобби   │  │ Комната  │  │ История  │  │ Детали встречи   │ │
│  │          │  │ (LiveKit)│  │  встреч  │  │ (транскрипт/     │ │
│  │          │  │          │  │          │  │  суммари)        │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Next.js API Routes                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │
│  │ /rooms   │  │/meetings │  │/webhook  │  │ /transcribe      │ │
│  │          │  │          │  │(LiveKit) │  │ /summarize       │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
        │                │              │                │
        ▼                ▼              ▼                ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  PostgreSQL  │  │   LiveKit    │  │ Parakeet V3  │  │  Claude API  │
│   (Prisma)   │  │   Server     │  │ (Rust/ONNX)  │  │              │
└──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘
                        │                   ▲
                        ▼                   │
                 ┌──────────────┐           │
                 │    MinIO     │───────────┘
                 │ (S3 storage) │  (аудио файлы)
                 └──────────────┘
```

## Структура проекта

```
tinkerdesk/
├── docker-compose.yml              # Оркестрация всех сервисов
├── .env.example                    # Переменные окружения
├── .gitignore
├── PLAN.md                         # Этот файл
│
├── app/                            # Next.js приложение
│   ├── src/
│   │   ├── app/
│   │   │   ├── page.tsx                    # Главная (лобби)
│   │   │   ├── layout.tsx                  # Root layout
│   │   │   ├── globals.css                 # Глобальные стили
│   │   │   ├── room/[roomId]/page.tsx      # Страница комнаты
│   │   │   ├── meetings/page.tsx           # История встреч
│   │   │   ├── meetings/[id]/page.tsx      # Детали встречи
│   │   │   └── api/
│   │   │       ├── rooms/route.ts          # CRUD комнат
│   │   │       ├── livekit/
│   │   │       │   ├── token/route.ts      # Генерация токенов
│   │   │       │   └── webhook/route.ts    # Обработка событий
│   │   │       ├── meetings/
│   │   │       │   ├── route.ts            # Список встреч
│   │   │       │   └── [id]/route.ts       # Детали встречи
│   │   │       ├── transcribe/route.ts     # Запуск транскрибации
│   │   │       └── summarize/route.ts      # Запуск суммаризации
│   │   │
│   │   ├── components/
│   │   │   ├── room/
│   │   │   │   └── VideoRoom.tsx           # Компонент голосовой комнаты
│   │   │   └── ui/                         # shadcn/ui компоненты
│   │   │       ├── button.tsx
│   │   │       ├── card.tsx
│   │   │       ├── input.tsx
│   │   │       ├── label.tsx
│   │   │       ├── tabs.tsx
│   │   │       ├── toast.tsx
│   │   │       ├── toaster.tsx
│   │   │       ├── use-toast.ts
│   │   │       └── scroll-area.tsx
│   │   │
│   │   └── lib/
│   │       ├── prisma.ts                   # Клиент Prisma
│   │       ├── livekit.ts                  # LiveKit SDK обёртка
│   │       ├── claude.ts                   # Claude API клиент
│   │       └── utils.ts                    # Утилиты (cn)
│   │
│   ├── prisma/
│   │   └── schema.prisma                   # Схема базы данных
│   │
│   ├── Dockerfile
│   ├── package.json
│   ├── tsconfig.json
│   ├── tailwind.config.js
│   ├── postcss.config.js
│   ├── next.config.js
│   └── .env.local.example
│
├── services/
│   ├── livekit/
│   │   ├── livekit.yaml                    # Конфигурация LiveKit
│   │   └── egress.yaml                     # Конфигурация записи
│   │
│   ├── transcriber/                        # [Legacy] Python/FastAPI (требует GPU)
│   │   ├── Dockerfile
│   │   ├── requirements.txt
│   │   └── main.py
│   │
│   └── transcriber-rs/                     # Сервис транскрибации (Rust)
│       ├── Cargo.toml                      # Зависимости Rust
│       ├── Dockerfile                      # Multi-stage build
│       └── src/
│           ├── main.rs                     # Axum HTTP сервер
│           ├── config.rs                   # Env конфигурация
│           ├── transcriber.rs              # parakeet-rs интеграция
│           ├── handlers.rs                 # HTTP handlers
│           ├── storage.rs                  # MinIO/S3 клиент
│           └── queue.rs                    # Redis очередь
│
└── scripts/
    └── setup.sh                            # Скрипт первоначальной настройки
```

## Схема базы данных

```prisma
model Room {
  id        String    @id @default(cuid())
  name      String
  createdAt DateTime  @default(now())
  meetings  Meeting[]
}

model Meeting {
  id           String        @id @default(cuid())
  roomId       String
  room         Room          @relation(...)
  startedAt    DateTime      @default(now())
  endedAt      DateTime?
  status       MeetingStatus @default(IN_PROGRESS)
  summary      String?       @db.Text
  participants Participant[]
  utterances   Utterance[]
  recordings   Recording[]
}

model Participant {
  id         String      @id @default(cuid())
  meetingId  String
  meeting    Meeting     @relation(...)
  name       String
  identity   String      // LiveKit identity
  joinedAt   DateTime    @default(now())
  leftAt     DateTime?
  utterances Utterance[]
  recordings Recording[]
}

model Utterance {
  id            String      @id @default(cuid())
  meetingId     String
  participantId String
  text          String      @db.Text
  startTime     Float       // Секунды от начала встречи
  endTime       Float
  createdAt     DateTime    @default(now())
}

model Recording {
  id            String      @id @default(cuid())
  meetingId     String
  participantId String
  fileUrl       String      // URL в MinIO
  fileName      String
  duration      Float
  transcribed   Boolean     @default(false)
}

enum MeetingStatus {
  IN_PROGRESS
  PROCESSING
  COMPLETED
  FAILED
}
```

## Статус реализации

### Выполнено ✅

| Этап | Задача | Статус |
|------|--------|--------|
| 1 | Docker Compose с PostgreSQL, MinIO, Redis, LiveKit | ✅ |
| 1 | Конфигурация LiveKit (livekit.yaml, egress.yaml) | ✅ |
| 2 | Инициализация Next.js проекта | ✅ |
| 2 | Настройка Tailwind CSS + shadcn/ui компоненты | ✅ |
| 2 | Prisma схема базы данных | ✅ |
| 3 | API для создания комнат (/api/rooms) | ✅ |
| 3 | API для генерации LiveKit токенов (/api/livekit/token) | ✅ |
| 3 | Webhook обработчик событий LiveKit | ✅ |
| 4 | Компонент VideoRoom для голосовой связи | ✅ |
| 4 | Страница комнаты (/room/[roomId]) | ✅ |
| 4 | Главная страница (лобби) | ✅ |
| 5 | Сервис транскрибации на Parakeet (Rust/ONNX) | ✅ |
| 5 | API для транскрибации (/api/transcribe) | ✅ |
| 6 | Интеграция Claude API для суммаризации | ✅ |
| 6 | API для суммаризации (/api/summarize) | ✅ |
| 7 | Страница истории встреч (/meetings) | ✅ |
| 7 | Страница деталей встречи (/meetings/[id]) | ✅ |
| 7 | Просмотр транскрипта и резюме | ✅ |

### Требует тестирования 🧪

- [ ] Интеграция LiveKit с записью (требует запуск Docker)
- [x] Работа Parakeet транскрибации (теперь на CPU через Rust/ONNX)
- [ ] End-to-end тест полного флоу встречи
- [ ] Webhook от LiveKit при окончании записи

### Рекомендации ⚠️

- [ ] Запустить `npm audit fix` в директории `app/` для устранения уязвимостей в зависимостях
- [ ] Обновить Next.js до последней версии (текущая 14.2.21 имеет предупреждение безопасности)

### Возможные улучшения 🔮

- [ ] Видео-связь (камеры участников)
- [ ] Шаринг экрана
- [ ] Чат во время встречи
- [ ] Скачивание записей
- [ ] Экспорт транскрипта в разные форматы
- [ ] Real-time транскрибация во время встречи
- [ ] Поиск по истории встреч
- [ ] Аутентификация пользователей (при необходимости)

## Переменные окружения

```env
# Database
DATABASE_URL=postgresql://tinkerdesk:tinkerdesk_secret@localhost:5432/tinkerdesk

# LiveKit
LIVEKIT_URL=ws://localhost:7880
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret123456789012345678901234567890
LIVEKIT_WEBHOOK_SECRET=whsec_secret123

# MinIO
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET=recordings

# Claude API (обязательно добавить свой ключ!)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Transcriber
TRANSCRIBER_URL=http://localhost:8001

# Redis
REDIS_URL=redis://localhost:6379

# Public (для клиента)
NEXT_PUBLIC_LIVEKIT_URL=ws://localhost:7880
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

## Запуск проекта

### Быстрый старт

```bash
# 1. Запустить скрипт настройки
./scripts/setup.sh

# 2. Добавить ANTHROPIC_API_KEY в app/.env.local

# 3. Запустить dev-сервер
cd app && npm run dev

# 4. Открыть http://localhost:3000
```

### Ручной запуск

```bash
# 1. Запустить инфраструктуру
docker compose up -d postgres redis minio minio-setup livekit livekit-egress

# 2. Установить зависимости
cd app && npm install

# 3. Сгенерировать Prisma клиент
npm run db:generate

# 4. Применить миграции
npm run db:push

# 5. Запустить dev-сервер
npm run dev
```

### Запуск транскрибатора (работает на CPU)

```bash
docker compose up transcriber -d
# Проверка статуса
curl http://localhost:8001/health
```

**Примечание:** Новый транскрибатор на Rust не требует NVIDIA GPU. Работает на любом CPU, включая Apple M1/M2/M3.

## Полезные команды

```bash
# Prisma Studio (GUI для БД)
cd app && npm run db:studio

# Пересоздать БД
cd app && npm run db:push --force-reset

# Логи LiveKit
docker compose logs -f livekit

# Логи транскрибатора
docker compose logs -f transcriber

# MinIO Console
# Открыть http://localhost:9001
# Логин: minioadmin / minioadmin123
```

## Ссылки

- [LiveKit Documentation](https://docs.livekit.io/)
- [NVIDIA Parakeet TDT V3](https://huggingface.co/nvidia/parakeet-tdt-0.6b-v3)
- [parakeet-rs (Rust библиотека)](https://github.com/altunenes/parakeet-rs)
- [Claude API](https://docs.anthropic.com/claude/reference/getting-started-with-the-api)
- [Next.js App Router](https://nextjs.org/docs/app)
- [Prisma Documentation](https://www.prisma.io/docs)
