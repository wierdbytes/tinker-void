# =============================================================================
# TinkerVoid Production Environment Variables
# =============================================================================
# Copy this file to .env.prod and fill in all values
# Run ./scripts/deploy.sh to auto-generate secure passwords
# =============================================================================

# -----------------------------------------------------------------------------
# DOMAIN CONFIGURATION (REQUIRED)
# -----------------------------------------------------------------------------
# Your domain name (without protocol)
DOMAIN=example.com

# Public URLs (used by browser)
NEXT_PUBLIC_LIVEKIT_URL=wss://livekit.example.com
NEXT_PUBLIC_APP_URL=https://example.com

# -----------------------------------------------------------------------------
# DATABASE (auto-generated by deploy.sh)
# -----------------------------------------------------------------------------
POSTGRES_USER=tinkervoid
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD
POSTGRES_DB=tinkervoid

# -----------------------------------------------------------------------------
# MINIO S3 STORAGE (auto-generated by deploy.sh)
# -----------------------------------------------------------------------------
MINIO_ROOT_USER=tinkervoid
MINIO_ROOT_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD
MINIO_BUCKET=recordings

# -----------------------------------------------------------------------------
# LIVEKIT (auto-generated by deploy.sh)
# -----------------------------------------------------------------------------
LIVEKIT_API_KEY=CHANGE_ME_GENERATE_API_KEY
LIVEKIT_API_SECRET=CHANGE_ME_GENERATE_SECRET_MIN_32_CHARS
LIVEKIT_WEBHOOK_SECRET=whsec_CHANGE_ME_GENERATE_SECRET

# -----------------------------------------------------------------------------
# PORTS (optional, defaults shown)
# -----------------------------------------------------------------------------
APP_PORT=3000
LIVEKIT_HTTP_PORT=7880
LIVEKIT_TCP_PORT=7881
LIVEKIT_UDP_PORT=7882

# -----------------------------------------------------------------------------
# RABBITMQ (auto-generated by deploy.sh)
# -----------------------------------------------------------------------------
RABBITMQ_USER=tinkervoid
RABBITMQ_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD

# -----------------------------------------------------------------------------
# TRANSCRIBER SETTINGS (optional)
# -----------------------------------------------------------------------------
WHISPER_MODEL_SIZE=large-v3
TRANSCRIBER_CPU_THREADS=4
TRANSCRIBER_LANGUAGE=ru
TRANSCRIBER_MEMORY_LIMIT=8G
TRANSCRIBER_MEMORY_RESERVATION=4G

# -----------------------------------------------------------------------------
# TRAEFIK REVERSE PROXY (optional)
# -----------------------------------------------------------------------------
# Set USE_TRAEFIK=true to enable Traefik integration
USE_TRAEFIK=false

# Host for the main application (e.g., tinkervoid.example.com)
TRAEFIK_HOST=tinkervoid.example.com

# Host for LiveKit WebSocket (e.g., livekit.example.com)
TRAEFIK_LIVEKIT_HOST=livekit.example.com

# Traefik certificate resolver name (default: le)
TRAEFIK_CERTRESOLVER=le

# -----------------------------------------------------------------------------
# ADMIN PANEL (auto-generated by deploy.sh)
# -----------------------------------------------------------------------------
# Secret key for /void admin panel access
ADMIN_SECRET_KEY=CHANGE_ME_GENERATE_SECURE_KEY
